# Migrações do Banco de Dados

Esta pasta contém as migrações para o banco de dados do sistema B-Manager, gerenciadas pelo Alembic através do Flask-Migrate.

## Comandos para Migrações

Para criar uma nova migração após alterações nos modelos:

```
flask db migrate -m "Descrição das alterações"
```

Para aplicar as migrações pendentes:

```
flask db upgrade
```

Para reverter a última migração:

```
flask db downgrade
```

Para mais informações sobre migrações, consulte a documentação do Flask-Migrate e Alembic. 